-- hub_dispatches is created in 20260721160000_hub_panel (later migration).
-- dispatch_no is indexed there via hub_dispatches_dispatch_no_key (UNIQUE).
-- No-op: kept so applied migration history stays valid for shadow DB replay.

SELECT 1;
